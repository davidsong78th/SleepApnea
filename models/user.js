class User {
    constructor(id, title, imageUri, document) {
        this.id = id
        this.title = title
        this.imageUri = imageUri
        this.document = document
    }
}

export default User